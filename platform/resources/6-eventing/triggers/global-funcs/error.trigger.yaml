apiVersion: eventing.knative.dev/v1
kind: Trigger
metadata:
  name: error
  namespace: global-funcs
spec:
  broker: default
  filter:
    attributes:
      type: error
  subscriber:
    ref:
      apiVersion: v1
      kind: Service
      name: error-handler
